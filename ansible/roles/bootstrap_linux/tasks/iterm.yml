---
- name: Install Iterm Shell Integration
  ansible.builtin.shell: |
    sudo su - admin -c 'curl -L https://iterm2.com/shell_integration/install_shell_integration.sh | bash'
  args:
    executable: /bin/bash